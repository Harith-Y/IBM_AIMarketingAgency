services:
  - type: web
    name: granite-api-service
    runtime: node
    rootDir: granite-api-service
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: OPENROUTER_API_KEY
        sync: false